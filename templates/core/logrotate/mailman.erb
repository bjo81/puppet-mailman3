<%= @logs_dir %>/*.log {
    missingok
    rotate <%= @rotate %>
    sharedscripts
    su <%= @username %> <%= @groupname %>
    compress
    notifempty
    <%= @rotate_every %>
    postrotate
        /bin/kill -HUP `cat <%= @pid_file %> 2>/dev/null` 2>/dev/null || true
    endscript
}
